name: Monthly Earnings Date Batch

on:
  # 毎月1日の日本時間20:00 (UTC 11:00) に自動実衁E  # ※決算日は頻繁には変わらなぁE��め月1回で十�E
  schedule:
    - cron: '0 11 1 * *'
  
  # 手動でボタンを押して実行できるようにする設宁E  workflow_dispatch:

jobs:
  run-earnings-batch:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: Run Fetch Earnings Dates
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          python fetch_earnings_dates.py
